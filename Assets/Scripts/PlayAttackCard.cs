using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayAttackCard : MonoBehaviour
{
/*
    public BattleSystem battleSystem;
    public BattleSystem battleSystem.BattleState;


    public void OnAttackButton()
    {
        if (state != battleSystem.BattleState.PLAYERTURN)
        {
            return;
        }

        if (playerUnit.unitEnergy == 0)
        {
            dialogueText.text = "Not enough energy";
            return;
        }

        StartCoroutine(PlayerAttack());
    }

    //Recuerda que la carta se debe destruir tras ser usada
*/
}
